/**
 * @namespace
 * @property {String} notify
 * @property {?Game} game
 * @property {?View} view
 * @property {?Grid} grid
 */
export const Config = {
  notify: 'console',
  game: null,
  view: null,
  grid: null
}

window.Config = Config
